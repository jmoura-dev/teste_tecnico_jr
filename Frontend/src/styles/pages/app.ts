import { styled } from '..'

export const Container = styled('div', {
  display: 'flex',
  flexDirection: 'column',
  margin: '0 auto',
  justifyContent: 'center',
  minHeight: '100vh',
  padding: '0 0.5rem',
  overflow: 'hidden',
})
